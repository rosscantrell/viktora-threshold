# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Generated in CI by the release workflow's Windows signing step (Azure Trusted
# Signing signCommand overlay). Never committed — values come from repo Variables.
src-tauri/tauri.signing.conf.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local-only ported Claude Code memory (may contain sensitive notes)
memory-bank/

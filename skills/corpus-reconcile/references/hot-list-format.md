# House hot-list format

> **PLACEHOLDER — fill this in from one real sample.**
>
> This file defines how a reconciled action-item list should be formatted for
> your team. It ships empty on purpose: the correct format is yours, and the
> details that matter (bullet style, where bold goes, how links render, spacing
> between items and sections) only become clear from a real example.
>
> **To activate:** paste one list that is formatted exactly the way you want it,
> and ask Claude to "capture this as my hot-list format." Claude will replace
> everything below with the concrete rules it observes. After that, every
> reconciled list comes out matching your sample without re-explaining.

## Format rules

_(none captured yet — see above)_

## Worked example

_(none captured yet — paste your sample to populate this)_

## Notes

- Keep this describing *format only* (structure, styling, spacing). The
  reconciliation logic — what goes in the list — lives in `../SKILL.md` and is
  intentionally independent of how the list looks.
- If different clients or document types need different formats, add a section
  per variant here, or split into sibling files (`hot-list-clientA.md`, etc.)
  and pick the right one when formatting.
